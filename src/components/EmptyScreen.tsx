import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const EmptyScreen = () => {
  return (
    <View>
      <Text>No data. Screen TO DO</Text>
    </View>
  );
};

export default EmptyScreen;

const styles = StyleSheet.create({});
